<main class="main">
  <div class="inner">
    <%# <% if user_signed_in? %>
      <div class="greeting">
        <%= "こんにちは、" %>
         <%= link_to "新規登録", new_user_registration_path, class: :greeting__link %>
      </div>
    <%# <% end %>
    <%# // ログインしているときは上記を表示する %>
    <div class="card__wrapper">
    <%# <%= render partial: 'prototypes/prototype', collection: @prototypes %>
    </div>
  </div>
</main>
